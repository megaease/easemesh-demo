apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    mesh.megaease.com/service-name: restaurant-mesh
    mesh.megaease.com/service-labels: release=refund-android
  name: restaurant-mesh-android
  namespace: mesh-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: restaurant-mesh-android
  template:
    metadata:
      labels:
        app: restaurant-mesh-android
    spec:
      containers:
      - name: restaurant-mesh-android
        image: megaease/consuldemo:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
        env:
        - name: SERVICE_NAME
          value: restaurant-mesh-android
        - name: ZIPKIN_SERVER_URL
          value: https://dev.megaease.cn/zipkin

---

apiVersion: v1
kind: Service
metadata:
  name: restaurant-mesh-android
  namespace: mesh-service
spec:
  selector:
    app: restaurant-mesh-android
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
