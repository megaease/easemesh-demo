package com.megaease.mesh.meshdemo;

import org.springframework.stereotype.Component;

// NOTE: It's useless for now.
public interface RestaurantService {
    @Component
    class RestaurantServiceImpl implements RestaurantService {
    }
}
